spring:
  application:
    name: daewoo2
  datasource: # mysql ??
    url: jdbc:log4jdbc:mysql://localhost:3306/db???serverTimezone=Asia/Seoul&characterEncoding=UTF-8    # ?? URL
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ???  # mysql ??
    password: ??  # mysql ?? ??

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.example.daewoo
  configuration:
    map-underscore-to-camel-case: true