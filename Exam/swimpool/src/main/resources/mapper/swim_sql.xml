<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjc813.swimpool.service.SwimMapper">

    <sql id="selectSql">
        SELECT
            id
             , name
             , lanes
             , lane_type
             , phone_number
             , addr
    </sql>

    <sql id="whereSql">
        <where>
            1 = 1
            <if test="name != null || name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="lanes != null || lanes != ''">
                AND lanes LIKE CONCAT('%', #{lanes}, '%')
            </if>
            <if test="laneType != null || laneType != ''">
                AND lane_type LIKE CONCAT('%', #{laneType}, '%')
            </if>
            <if test="phoneNumber != null || phoneNumber != ''">
                AND phone_number LIKE CONCAT('%', #{phoneNumber}, '%')
            </if>
            <if test="addr != null || addr != ''">
                AND addr LIKE CONCAT('%', #{addr}, '%')
            </if>
        </where>
    </sql>

    <insert id="insert" parameterType="SwimDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO
            swimpool_tbl
            ( name
            , lanes
            , lane_type
            , phone_number
            , addr )
        VALUES
            ( #{name}
            , #{lanes}
            , #{laneType}
            , #{phoneNumber}
            , #{addr} )
    </insert>

    <select id="selectList" resultType="SwimDto">
        SELECT
            id
            , name
            , lanes
            , lane_type
            , phone_number
            , addr
        FROM
            swimpool_tbl
    </select>

    <select id="selectSearch" parameterType="SwimSearchDto" resultType="SwimDto">
        <include refid="selectSql" />
        FROM
            swimpool_tbl
        <include refid="whereSql" />
    </select>

    <select id="selectOne" parameterType="Long" resultType="SwimDto">
        SELECT
            id
            , name
            , lanes
            , lane_type
            , phone_number
            , addr
        FROM
            swimpool_tbl
        WHERE
            id = #{id}
    </select>

    <update id="update" parameterType="SwimDto">
        UPDATE
            swimpool_tbl
        SET
             name = #{name}
            , lanes = #{lanes}
            , lane_type = #{laneType}
            , phone_number = #{phoneNumber}
            , addr = #{addr}
        WHERE
            id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM
            swimpool_tbl
        WHERE
            id = #{id}
    </delete>
</mapper>